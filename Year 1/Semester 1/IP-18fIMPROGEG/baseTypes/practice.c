



// % d to accept input of integers.
// % ld to  accept input of long integers
// % lld to accept input of long long integers
// % f to accept input of real number.
// % c to accept input of character types.
// % s to accept input of a string.

#include<stdio.h>
#include<math.h>


int main() {
    // 2.4.1.Declare an int variable and print its value.Notice what the initial value of a variable is.
    // int n;
    // scanf("%d", &n);
    // printf("%d", n);

    // 2.4.2.Assign a new value to the previous variable(it can be a constant value or can be read from the user with scanf()) and print if the value is even or odd.
    // int n;
    // scanf("%d", &n);
    // n % 2 == 0 ? printf("%s", "It is even number") : printf("%s", "It is odd number");
    // printf("%s", n % 2 == 0 ? "Even" : "Odd");

    // 2.4.3.Print if the number is positive or negative or zero.
    // int n;
    // scanf("%d", &n);
    // if (n == 0) {
    //     printf("%s", "Zero");
    // }
    // else if (n > 0) {
    //     printf("%s", "Positive");
    // }
    // else {
    //     printf("%s", "Negative");
    // }

    // 2.4.4.Try to assign a floating point number, a char, a boolean and a string to the int variable.If the assignment is successful then print its value.Use - W, -Wall, and -Wextra flags during compilation.
    // float f;
    // int n;
    // char c;
    // char arr[32];
    // scanf("%f", &f);
    // scanf("%d", &n);
    // scanf(" %c", &c);
    // scanf("%s", arr);

    // printf("%f\n", f);
    // printf("%d\n", n);
    // printf("%c\n", c);
    // printf("%s\n", arr);

    // 2.4.6.Determine the greatest and the smallest integer numbers(sizeof).Assign the greatest integer to a variable and add one.Check what happens.
    // int n = 0;
    // n = sizeof n;
    // n += 1;
    // printf("%d", n);
    // return 0;


    // 2.5.scanf
    // 2.5.1.Determine the average of two integers.
    // int a = 10, b = 21;
    // int average = (a + b) / 2;
    // float faverage = (a + b) * 1.0 / 2;
    // printf("%d\n", average);
    // printf("%.2f\n", faverage);


    // 2.5.2.Create a program where you compute the area of a square and a circle.Get the length of the square and the radius of the circle from the user.
    // Use the function scanf("%f", &a).Use the operator & which returns the address of a variable.The value of pi should be 3.1415.
    // float pi = 3.1415, a;
    // scanf("%f", &a);
    // float ans = pi * pow(a, 2);
    // printf("%.2f", ans);

    // 2.5.3.Create a Fahrenheit - Celsius converter(C = (F - 32) / 1.8).Write[-20; 200] interval Fahrenheit values with a scale of 10, and the corresponding degree Celsius.
    // for (int i = -20; i <= 200; i += 10) {
    //     float ans = (i - 32) / 1.8;
    //     printf("%.2f\n", ans);
    // }

}